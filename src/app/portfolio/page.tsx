import { RouteTemplate } from "@/components/route-template";

export default function PortfolioPage() {
  return (
    <RouteTemplate
      title="Portfolio"
      description="A premium showcase of selected work across AI, web systems, digital products, and business projects, mapped to outcomes and execution depth."
    />
  );
}
